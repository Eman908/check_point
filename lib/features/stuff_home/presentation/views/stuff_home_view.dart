import 'package:flutter/material.dart';

class StuffHomeView extends StatelessWidget {
  const StuffHomeView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text("This is Stuff")));
  }
}
